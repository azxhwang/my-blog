# Superpowers：让 AI 编程助手从"野生"变"专业"的完整指南

> 你是否遇到过这种情况：让 Claude/Cursor 写代码，它一顿操作猛如虎，结果代码风格混乱、没有测试、Git 历史一团糟？
> 
> Superpowers 就是来解决这个问题的——它给 AI 装上了"职业程序员的工作流"。

---

## 一、Superpowers 是什么？

**一句话定义**：Superpowers 是一套给 AI 编码代理（Coding Agents）用的"标准化作业程序"（SOP）。

想象你是一名新入职的程序员，公司会给你一本《开发规范手册》，告诉你：
- 怎么写设计文档
- 怎么开分支
- 怎么写测试
- 怎么做 Code Review
- 怎么合并代码

**Superpowers 就是 AI 的《开发规范手册》**。

它由 Jesse Vincent（Prime Radiant）开源，支持 Claude Code、Cursor、Codex、Gemini CLI 等主流 AI 编程工具。

---

## 二、为什么需要 Superpowers？

### 🚨 没有 Superpowers 的 AI 编程现状

| 问题 | 真实场景 |
|------|----------|
| **代码质量不稳定** | 这次生成的代码很好，下次同样的需求却风格迥异 |
| **不写测试** | AI 直接写实现，测试？什么测试？ |
| **Git 历史灾难** | 所有修改堆在一个 commit，或者 commit 信息毫无意义 |
| **缺乏规划** | 想到哪写到哪，中途发现架构问题只能推倒重来 |
| **不自我审查** | 写完了就完了，从不检查是否有 bug 或优化空间 |

### ✅ 有了 Superpowers 之后

```
用户：帮我实现一个用户登录功能

AI：好的，让我按照 Superpowers 流程来处理：

[阶段 1: Brainstorming]
首先，我需要澄清几个设计问题：
1. 使用 JWT 还是 Session？
2. 密码加密算法偏好？
3. 需要支持 OAuth 吗？

[阶段 2: Git Worktree]
创建独立工作分支：feature/user-login
验证当前测试基线通过 ✓

[阶段 3: Writing Plans]
将任务拆分为：
- 任务 1: 设计数据库用户表 (2分钟)
- 任务 2: 实现密码加密工具 (3分钟)
- 任务 3: 实现登录 API + 测试 (5分钟)
- 任务 4: 实现 JWT 签发 (3分钟)

[阶段 4: TDD 开发]
先写测试 → 测试失败(RED) → 写实现 → 测试通过(GREEN) → 重构

[阶段 5: Code Review]
自我审查发现：密码错误时不应提示"密码错误"，应统一提示"用户名或密码错误"（防止枚举攻击）

[阶段 6: 完成分支]
所有测试通过 ✓
提供选项：创建 PR / 本地合并 / 丢弃
```

**核心改变**：AI 从"野生程序员"变成了"有纪律的专业开发者"。

---

## 三、核心概念：7 阶段工作流

Superpowers 的核心是 **7 个标准化阶段**，每个阶段都有明确的输入、输出和检查点。

### 阶段 1: Brainstorming（头脑风暴）

**目的**：把模糊的需求变成清晰的设计方案。

**AI 会做什么**：
- 用苏格拉底式提问澄清需求
- 分段展示设计方案（不是一次性甩给你一大段）
- 确认技术选型

**示例对话**：
```
用户：做个文件上传功能

AI：好的，关于文件上传，我需要确认几个点：

1. 文件类型限制？（图片/文档/任意文件）
2. 大小限制？
3. 存储位置？（本地磁盘/S3/云存储）
4. 是否需要病毒扫描？
5. 上传后是否需要生成缩略图？

请告诉我你的偏好，然后我会给出完整设计方案。
```

### 阶段 2: Using Git Worktrees（Git 工作树）

**目的**：隔离开发环境，不污染主分支。

**AI 会做什么**：
- 创建独立的工作分支
- 运行项目设置脚本
- 验证当前测试基线（确保开始之前所有测试通过）

**为什么重要**：
> 想象一下，AI 在主分支直接改代码，改到一半你发现方向错了，想回退——结果发现主分支已经一团糟。

### 阶段 3: Writing Plans（编写计划）

**目的**：把大任务拆成可执行的小步骤。

**关键规则**：
- 每个任务 2-5 分钟完成
- 包含精确的文件路径
- 包含验证步骤（怎么算"完成"）

**示例计划**：
```markdown
## 任务 1: 创建用户模型
- 文件: `/src/models/User.js`
- 内容: 定义用户 Schema（用户名、密码哈希、创建时间）
- 验证: 运行 `npm test models/User.test.js` 通过

## 任务 2: 实现密码加密
- 文件: `/src/utils/crypto.js`
- 内容: bcrypt 封装，提供 hashPassword 和 comparePassword
- 验证: 运行 `npm test utils/crypto.test.js` 通过
```

### 阶段 4: Executing Plans（执行计划）

**两种模式**：

**模式 A: 子代理驱动（Subagent-Driven）**
- 主 AI 把任务分配给子 AI
- 子 AI 独立完成任务
- 适合大规模并行开发

**模式 B: 批量执行（Executing Plans）**
- 当前 AI 按顺序执行任务
- 每完成一个任务标记为 ✓
- 适合中小规模任务

### 阶段 5: Test-Driven Development（测试驱动开发）

**这是 Superpowers 的灵魂。**

**TDD 循环**：
```
RED（红） → GREEN（绿） → REFACTOR（重构）
  ↓           ↓              ↓
写测试       写最简实现      优化代码
（失败）     （通过测试）    （保持测试通过）
```

**AI 必须遵守的规则**：
1. **先写测试，再写实现**（没有例外）
2. 测试必须失败（RED）才能写实现
3. 实现只需让测试通过（不求完美）
4. 测试通过后才能重构

**示例**：
```javascript
// 步骤 1: 写测试（RED）
// /tests/calculator.test.js
test('add(2, 3) should return 5', () => {
  expect(add(2, 3)).toBe(5);  // 失败！add 函数还不存在
});

// 步骤 2: 写最简实现（GREEN）
// /src/calculator.js
const add = (a, b) => a + b;  // 最简单实现

// 步骤 3: 重构（保持测试通过）
// 添加类型检查、错误处理等
const add = (a, b) => {
  if (typeof a !== 'number' || typeof b !== 'number') {
    throw new TypeError('Arguments must be numbers');
  }
  return a + b;
};
```

### 阶段 6: Requesting Code Review（代码审查）

**目的**：让 AI 自我审查，发现潜在问题。

**审查维度**：
- **安全性**：SQL 注入、XSS、敏感信息泄露
- **性能**：N+1 查询、内存泄漏、算法复杂度
- **可维护性**：代码重复、命名清晰、注释充分
- **测试覆盖**：边界条件、异常路径是否测试

**严重程度分级**：
- 🔴 **Critical**：必须修复（安全漏洞、数据丢失风险）
- 🟡 **Warning**：建议修复（性能问题、代码异味）
- 🟢 **Suggestion**：可选优化（命名、注释风格）

### 阶段 7: Finishing a Branch（完成分支）

**目的**：优雅地结束开发周期。

**AI 会做什么**：
1. 运行完整测试套件，确保全部通过
2. 检查代码覆盖率
3. 提供三个选项：
   - **创建 Pull Request**：生成 PR 描述，推送到远程
   - **本地合并**：合并到主分支，删除特性分支
   - **丢弃**：放弃当前分支的所有修改（确认三次）

---

## 四、5 分钟快速上手

### 步骤 1：安装 Superpowers

根据你的 AI 工具选择安装方式：

**Claude Code 用户**：
```bash
/plugin install superpowers@claude-plugins-official
```

**Cursor 用户**：
```bash
/add-plugin superpowers
```

**Gemini CLI 用户**：
```bash
gemini extensions install https://github.com/obra/superpowers
```

**Codex/OpenCode 用户**：
查看仓库中的 `.codex/INSTALL.md` 或 `.opencode/INSTALL.md`

### 步骤 2：验证安装

启动新会话，输入：
```
帮我规划一个新功能
```

如果 AI 自动进入 **Brainstorming** 模式（开始提问澄清需求），说明安装成功。

### 步骤 3：第一次实战

让我们做一个简单的实战：实现一个待办事项（Todo）列表。

**输入**：
```
帮我实现一个 Todo List 的增删改查功能
```

**观察 AI 的行为**：
1. ✅ 是否先提问澄清需求？（Brainstorming）
2. ✅ 是否创建独立分支？（Git Worktree）
3. ✅ 是否给出任务拆分计划？（Writing Plans）
4. ✅ 是否先写测试再写实现？（TDD）
5. ✅ 是否自我审查代码？（Code Review）
6. ✅ 是否提供合并选项？（Finishing）

如果都做到了，恭喜你，Superpowers 已经生效！

---

## 五、底层设计思路解析

### 设计哲学 1：系统化优于临时方案

**问题**：为什么不让 AI 自由发挥？

**答案**：人类的"自由发挥"建立在多年工程经验上，AI 没有这种直觉。

Superpowers 把**隐性知识**（怎么写好代码）变成**显性流程**（必须按步骤来）。

类比：
- 没有 Superpowers = 让实习生直接写生产代码
- 有 Superpowers = 给实习生一本《入职手册》，按手册一步步来

### 设计哲学 2：测试是唯一的真理

**核心理念**：代码能运行不代表正确，测试通过才代表正确。

这就是为什么 Superpowers **强制 TDD**：
- 先定义"正确"（写测试）
- 再实现"正确"（写代码）
- 持续验证"正确"（运行测试）

**副作用**：
- 代码天然可测试（测试先行，不是事后补）
- 重构有安全保障（测试不通过就不能交付）
- 文档即测试（测试用例就是使用示例）

### 设计哲学 3：小步快跑，快速反馈

**任务拆分原则**：2-5 分钟一个任务。

**为什么？**
- AI 的上下文窗口有限，小任务减少"失忆"
- 快速完成带来正反馈，保持动力
- 出错时回滚成本低

**类比**：
- 大任务 = 写 1000 行代码再测试，发现架构错误，全部重写
- 小任务 = 每 20 行验证一次，错误立即发现，立即修复

### 设计哲学 4：隔离与可逆

**Git Worktree 的意义**：
- 实验性代码不影响主分支
- 随时可以"丢弃"（沉没成本低）
- 多人协作时减少冲突

**代码审查的意义**：
- 引入"暂停点"，强制反思
- 发现 AI 的"幻觉"（看起来对，实际有问题）
- 模拟团队协作（自我审查 → 他人审查）

### 设计哲学 5：可组合的技能

Superpowers 的 `skills/` 目录是可组合的模块：

```
skills/
├── testing/           # TDD 技能
├── debugging/         # 系统化调试
├── collaboration/     # 头脑风暴、计划编写
├── git/               # 工作树管理
└── meta/              # 编写新技能（技能生成技能）
```

**好处**：
- 按需加载（不需要 TDD 的项目可以禁用）
- 社区扩展（可以编写自己的技能）
- 版本隔离（技能更新不影响框架核心）

---

## 六、实际应用场景

### 场景 1：个人开发者（你）

**痛点**：用 AI 写代码很快，但维护起来很痛苦。

**Superpowers 价值**：
- 强制测试 → 代码可维护
- 强制 Code Review → 减少低级错误
- 规范 Git 流程 → 历史清晰，方便回滚

**使用建议**：
- 所有新功能都用 Superpowers 流程
- 把生成的测试当作"活文档"
- 定期回顾 Code Review 报告，总结常见错误

### 场景 2：小团队（2-5 人）

**痛点**：每个人用 AI 的风格不同，代码风格混乱。

**Superpowers 价值**：
- 统一 AI 的使用规范
- 生成的代码风格一致
- Code Review 报告可作为团队学习材料

**使用建议**：
- 团队约定：所有 AI 生成的代码必须经过 Superpowers 流程
- 共享自定义技能（如公司内部的 API 规范）
- 把 Superpowers 当作"初级开发者的导师"

### 场景 3：开源项目维护者

**痛点**：PR 质量参差不齐，审查成本高。

**Superpowers 价值**：
- 贡献者用 Superpowers 生成 PR，质量有保障
- 测试覆盖率高，减少回归 bug
- 规范的 Git 历史，方便追溯

**使用建议**：
- 在 CONTRIBUTING.md 中推荐 Superpowers
- 提供项目特定的技能配置（如测试框架选择）

---

## 七、进阶技巧

### 技巧 1：自定义技能

Superpowers 支持编写自定义技能。

**示例**：你们团队使用特定的日志规范，可以创建一个 `skills/team-logging/`：

```yaml
# skills/team-logging/skill.yaml
name: team-logging
description: 团队日志规范
triggers:
  - "添加日志"
  - "记录日志"
steps:
  - 使用统一的 logger 实例（import { logger } from '@/utils/logger'）
  - 日志级别规范：DEBUG（开发）、INFO（正常流程）、WARN（可恢复错误）、ERROR（需处理）
  - 禁止直接使用 console.log
```

### 技巧 2：禁用某些阶段

某些场景不需要完整流程，可以跳过：

```
帮我快速原型一个功能，跳过 TDD，但要保留 Code Review
```

### 技巧 3：批量任务并行

大规模重构时，使用子代理模式：

```
我需要重构 20 个组件，使用子代理并行处理，每个代理负责 4 个组件
```

---

## 八、常见问题 FAQ

**Q: Superpowers 会减慢开发速度吗？**

A: 短期会（因为要多写测试、多走流程），长期会**加快**（因为 bug 少、可维护性强、重构安全）。

**Q: 所有项目都适合用 Superpowers 吗？**

A: 探索性原型、一次性脚本可以跳过，生产代码强烈建议用。

**Q: Superpowers 和 Cursor/Claude 的 Composer 有什么区别？**

A: Composer 是"工具"，Superpowers 是"方法论"。Composer 提供能力，Superpowers 规范如何使用这些能力。

**Q: 可以只用部分功能吗？**

A: 可以，Superpowers 的技能是模块化的，可以按需启用。

**Q: 团队有人不用 Superpowers 怎么办？**

A: Superpowers 是辅助工具，不是强制规范。建议从"展示价值"开始：用 Superpowers 生成的代码质量明显更高，自然会有更多人愿意尝试。

---

## 九、总结

Superpowers 不是又一个 AI 编程工具，而是**AI 编程的工业化标准**。

它解决的核心问题：**如何让 AI 生成的代码从"能跑"变成"专业"**。

### 关键收获

1. **7 阶段工作流** = 把混乱的 AI 编程变成可预测的工程流程
2. **TDD 强制** = 用测试定义正确性，而非"看起来对"
3. **小步快跑** = 减少 AI 失忆风险，快速反馈
4. **自我审查** = 让 AI 扮演"严格的代码审查者"

### 下一步行动

1. **今天**：安装 Superpowers，验证是否生效
2. **本周**：用一个真实小功能走完完整流程
3. **本月**：团队内推广，收集反馈，自定义技能

---

## 参考资源

- **GitHub 仓库**: https://github.com/obra/superpowers
- **官方文档**: 仓库 `docs/` 目录
- **社区**: GitHub Discussions 和 Discord

---

> 💡 **最后的话**：AI 编程工具越来越强大，但"工具"和"专业开发者"之间还差一个"方法论"。Superpowers 就是填补这个差距的桥梁。

---

*本文基于 Superpowers 开源项目编写，遵循 MIT 许可证。*
